services:
  - type: web
    name: x-media-saver
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 300 --workers 1 --worker-class gevent --worker-connections 100 app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: RENDER
        value: "true"
